# Copy to .env for local overrides. Do not commit .env.

# Optional path overrides (used once ingest and warehouse code exist)
DATA_DIR=data
DUCKDB_PATH=data/warehouse/melbourne_footfall.duckdb

# Add credentials only after a source is verified against the live API.
# Do not invent endpoint URLs or dataset identifiers here.
